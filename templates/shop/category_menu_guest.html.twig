<div id="category">
    <ul>
        <li>
            <a href="{{constant('shop\\Bootstrap::ENTRY_URL')}}list_guest.php">全て</a>
        </li>
            {% for value in cateArr %}
        <li>
            <a href="{{constant('shop\\Bootstrap::ENTRY_URL')}}list_guest.php?ctg_id={{value.ctg_id}}">{{value.category_name}}</a>
        </li>
            {% endfor %}
    </ul>
</div>