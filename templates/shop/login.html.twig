<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <link href="{{constant('shop\\Bootstrap::APP_URL')}}css/shop.css" rel="stylesheet" type="text/css" />
        <title>ログイン画面</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/bxslider/4.2.12/jquery.bxslider.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
        <script src="https://cdn.jsdelivr.net/bxslider/4.2.12/jquery.bxslider.min.js"></script>

        <script>
            $(document).ready(function(){
                $('.slider').bxSlider({
                    auto: true,
                    mode: 'fade',
                    speed: 1000,
                    slideWidth: 400
                });
            });
  </script>
    </head>
    <body>
        <header> 
            <h2>きぬ太商店</h2>
        </header>
        <div class="wrapper">
            ログインIDとパスワードを入力してください
            <form method="post" action="login.php">
                <table>
                    <tr>
                        <th>ログインID<span class="red">*</span></th>
                        <td>
                            <input type="text" name="login_id" value="{{dataArr.login_id}}" />
                            {% if errArr.login_id | length > 0 %}<br><span class="red">{{ errArr.login_id }}</span>{% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th>パスワード<span class="red">*</span></th>
                        <td>
                            <input type="password" name="pass1" value="" />
                            {% if errArr.pass1 | length > 0 %}<br><span class="red">{{ errArr.pass1 }}</span>{% endif %}
                            {% if errMsg | length > 0 %}<br><span class="red">{{ errMsg }}</span>{% endif %}
                        </td>
                    </tr>
                </table> 
                <input type="submit" name="login" value="ログイン" /><br>
            </form>
            <div>
                <ul>
                    <li><a href="{{constant('shop\\Bootstrap::ENTRY_URL')}}regist.php">新規登録</a></li>
                    <li><a href="{{constant('shop\\Bootstrap::ENTRY_URL')}}list_guest.php">商品一覧</a></li>
                    <li><a href="{{constant('shop\\Bootstrap::ENTRY_URL')}}form.php">お問合せ</a></li>
                </ul>
            <div>
            <div class="slider">
                <div><img src="./../shop/images/masashi.jpg"></div>
                <div><img src="./../shop/images/ichigos.jpg"></div>
                <div><img src="./../shop/images/hachiya.jpg"></div>
                <div><img src="./../shop/images/seiryu.jpeg"></div>
            </div>
        </div>
        <footer>
            <p><small>Copyright &copy; KinutaShouten All Rights Reserved.</small></p>
        </footer>
    </body>
</html>